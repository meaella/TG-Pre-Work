
for (let counter = 5; counter < 11; counter++) {
  console.log(counter);
}

/* I learned about "For" loop includes an iterator variable that usually appears in all three expressions:
1. The iterator variable is initialized.
2. The iterator variable checked against the stopping condition.
3. The iterator variable assigned a new value on each loop iteration. */
