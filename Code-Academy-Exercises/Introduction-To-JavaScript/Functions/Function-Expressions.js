
const plantNeedsWater = function(day, plantNeedsWater){
  if (day === 'Wednesday'){
    return true;
  }
};

// I learned that a function expression is often stored in a variable in order to refer to it
