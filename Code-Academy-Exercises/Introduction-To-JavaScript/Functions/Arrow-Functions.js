
const plantNeedsWater = day => {
  if (day === 'Wednesday') {
    return true;
  } else {
    return false;
  }
};

/* I learned about the arrow functions that remove the keyword "function",
instead, first includes the parameters inside the ( ) and then add an arrow => that points to the function body surrounded in { } */
